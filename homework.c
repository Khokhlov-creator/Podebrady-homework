#include <stdio.h>

int main()
{
	int a,b;
	char c;
	printf("Insert two integer numbers and one char delimeter beetwen inputs is space : ");
	scanf("%d %d %c", &a, &b, &c);
	printf("Your input %d, %d, %c", a, b, c);
	return 0;
}
